<div class="banner-logo">
  <%= image_tag "SVG/Banner_logo.svg" %>
</div>
<%= render 'shared/navbar_home' %>
<div class="banner-image">
  <%= image_tag "grange.jpg" %>
</div>
<% if Product.where.not(discount: nil) %>
  <div class="banner-discount">
    <div>
      <%= image_tag "SVG/Hand/Hand_open.svg" %>
    </div>
    <div>
      <h4>Les promotions du moment</h4>
      <% @products.where.not(discount: nil).each do |p| %>
        <%= p.title %>
      <% end  %>
    </div>
    <div>
      <%= image_tag "SVG/Hand/Hand_open.svg" %>
    </div>
  </div>
<% end %>
<% @rayons.each do |rayon| %>
  <div class="banner-items">
    <div class="banner-items-title">
      <h4><%= rayon.title %></h4>
    </div>
    <div class="banner-category">
      <% rayon.categories.each do |c| %>
        <div class="banner-category-card">
          <%= link_to rayon_path(rayon) do %>
            <%= c.title %>
            <%#= cl image_tag c.products.first.main_photo %>
          <% end  %>
        </div>
      <% end  %>
    </div>
    <div class="banner-items-link">
      <h4>
        <%= link_to "Voir tout mes produits", rayon_path(rayon) %>
      </h4>
    </div>
  </div>
<% end %>
<div class="banner-items">
  <div class="banner-items-title">
    <h4>Coffrets</h4>
  </div>
  <div class="banner-category">
    <% @coffrets.each do |c| %>
      <div class="banner-category-card">
        <%= link_to coffrets_path do %>
          <%= c.title %>
          <%#= cl image_tag c.products.first.main_photo %>
        <% end  %>
      </div>
    <% end  %>
  </div>
  <div class="banner-items-link">
    <h4>
      <%= link_to "Voir tout mes coffrets", coffrets_path %>
    </h4>
  </div>
</div>
<div class="banner-items">
  <div class="banner-items-title">
    <h4>Formations</h4>
  </div>
  <div class="banner-category">
    <% @formations.each do |f| %>
      <div class="banner-category-card">
        <%= link_to coffrets_path do %>
          <%= f.title %>
          <%#= cl image_tag c.products.first.main_photo %>
        <% end  %>
      </div>
    <% end  %>
  </div>
  <div class="banner-items-link">
    <h4>
      <%= link_to "Voir toutes mes formations", formations_path %>
    </h4>
  </div>
</div>
