<div class="dashboard-container">
  <div class="dashboard-title">
    <h2>Utilisateurs</h2>
  </div>
  <table>
    <colgroup span="4" class="columns"></colgroup>
      <tr class= "table-headers">
        <th>Prénom</th>
        <th>Nom</th>
        <th>email</th>
        <th>Admin ?</th>
        <th>Log out ?</th>
        <th>Enregistrer</th>
      </tr>
      <% @users.each do |user| %>
      <tr class="table-lines">
        <th><%= user.first_name %></th>
        <th><%= user.last_name %></th>
        <th><%= user.email %></th>
        <%= simple_form_for(user_registration_path(user)) do |f| %>
        <th><%= f.input :admin, as: :boolean, label: false, method: :post %></th>
        <th><%= link_to "Log out", destroy_user_session_path, method: :delete %></th>
        <th>
            <%= f.button :submit, "Enregistrer les modifications pour cet utilisateur" %>
        </th>
        <% end %>
      </tr>
      <% end %>
    </table>
</div>
